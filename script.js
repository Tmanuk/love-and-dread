
document.addEventListener('DOMContentLoaded', () => {
  const game = document.getElementById('game');
  game.innerHTML = '<p>Character powers, syncing, and cinematic sequences are now active.</p>';
});
